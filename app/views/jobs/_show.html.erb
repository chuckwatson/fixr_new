 <li id="<%= job.id %>">
      <%= job.name %> <%= job.price %> <%= link_to "", shop_job_path(@shop, job), method: :delete, remote: true, class: "fa fa-trash", style: "float:right; color:#ff726f" %>
 </li>
